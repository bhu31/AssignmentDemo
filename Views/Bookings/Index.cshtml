@model IEnumerable<Assignment.Models.Booking>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "BookingClass")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UnitsId)
        </th>
        
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.Customer_id)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Class.Id)
        </th>


        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.UnitsId)
    </td>
    @*<td>
            @Html.DisplayFor(modelItem => item.Customer_id)
        </td>*@
    <td>
        @Html.DisplayFor(modelItem => item.Class.Id)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
    </td>
</tr>
}

</table>
